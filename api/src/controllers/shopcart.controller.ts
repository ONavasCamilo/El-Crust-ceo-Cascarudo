import { Request, Response } from "express";
import { SessionRequest } from "../interfaces/auth.interface";

export const addProductController = (req: SessionRequest, res: Response) => {
    // const { shopcart } = req.session;
}